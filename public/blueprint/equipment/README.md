# Public blueprint equipment assets

Place approved SVG or PNG files here when replacing inline illustrations.

Example: `keyboard.svg`, `console.svg`

Wire assets in `components/blueprint/graphics/asset-sources.ts`.

See `/docs/reference/equipment/README.md`.
