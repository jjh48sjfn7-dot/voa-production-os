import { BlueprintPreviewContent } from "@/components/blueprint/BlueprintPreviewContent";

export const metadata = {
  title: "Master Church Blueprint | VOA Production OS",
  robots: { index: false, follow: false },
};

export default function BlueprintPreviewPage() {
  return <BlueprintPreviewContent />;
}
